import Foundation

enum AppConstants {
    static let appGroupIdentifier = "group.com.subhudas.StorageWidget"
    static let settingsKey = "storage-widget-settings"
    static let defaultRefreshInterval: TimeInterval = 60
    static let minimumRefreshInterval: TimeInterval = 10
    static let maximumRefreshInterval: TimeInterval = 200
}
